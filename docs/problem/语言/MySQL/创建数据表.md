# 创建数据表

=== "sql"

    ```sql
    create table Student (
        name varchar(20),
        id varchar(20),
        sex tinyint,
        age smallint,
        score decimal(5, 2),
        average decimal(5, 2)
    );
    ```